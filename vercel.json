{
  "framework": "nextjs",
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 60
    }
  },
  "buildCommand": "DATABASE_URL='postgresql://localhost:5432/postgres' npx prisma generate && next build",
  "installCommand": "npm ci"
}