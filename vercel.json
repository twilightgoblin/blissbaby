{
  "framework": "nextjs",
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 60
    }
  },
  "buildCommand": "npx prisma generate && npx prisma migrate deploy && next build",
  "installCommand": "npm ci"
}