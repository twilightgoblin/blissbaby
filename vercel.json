{
  "framework": "nextjs",
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 60
    }
  },
  "buildCommand": "npx prisma generate && next build",
  "installCommand": "npm ci",
  "env": {
    "SKIP_ENV_VALIDATION": "1"
  }
}